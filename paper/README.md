# 论文目录

- `v1/`：最早论文稿存档，仅作历史对照。
- `v2/`：已完成、可编译并已提交的 retrieval-robustness 稳定稿。
- `v3/`：基于导师意见重构的新稿；当前先以中文论证和提纲启动。
- `scripts/`：V2 的图表和实验结果生成脚本。
- `PAPER_DISCUSSION_GUIDE_zh.md`：跨版本的写作决策与 V3 大修改订蓝图。

当前写作从 `v3/` 开始；需要查阅或复现实验时使用 `v2/`，不要混用两套正文和图片路径。
